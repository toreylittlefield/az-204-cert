var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/', function (req, res, next) {
  res.render('index', { title: 'Azure Web App with Github CI/CD pipeline' });
});

module.exports = router;
